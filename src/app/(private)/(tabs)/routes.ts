export enum ROOT_STACK {
  public = '(public)',
  private = '(private)',
  onboarding = 'index',
}

export enum PUBLIC_STACK {
  sign_in = 'sign-in',
  sign_up = 'sign-up',
}

export enum PRIVATE_STACK {
  tab = '(tabs)',
}

export enum TAB_BAR_STACK {
  HOME = 'index',
  SEARCH = 'search',
  CATEGORY = 'category',
  PROFILE = 'profile',
}
