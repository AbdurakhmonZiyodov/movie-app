export enum ROOT_STACK {
  AUTH = "AUTH",
  HOME = "HOME",
  ONBOARDING = "ONBOARDING",
}

export enum BOTTOM_BAR_STACK {
  HOME = "index",
  SEARCH = "search",
  CATEGORY = "category",
  PROFILE = "profile",
}

export enum REGISTER_STACK {
  LOG_IN = "LOG_IN",
  SIGN_UP = "SIGN_UP",
}
