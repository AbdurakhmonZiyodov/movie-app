export enum ROOT_STACK {
  tabs = "(tabs)",
  onboarding = "index",
  signIn = "sign-in",
}

export enum TAB_BAR_STACK {
  HOME = "index",
  SEARCH = "search",
  CATEGORY = "category",
  PROFILE = "profile",
}

// export enum REGISTER_STACK {
//   LOG_IN = "LOG_IN",
//   SIGN_UP = "SIGN_UP",
// }
