export enum ROOT_STACK {
  public = '(public)',
  private = '(private)',
  onboarding = 'index',
}

export enum PUBLIC_STACK {
  login = 'login',
  otp = 'otp',
  profile = 'profileInfo',
}

export enum PRIVATE_STACK {
  tab = '(tabs)',
}

export enum TAB_BAR_STACK {
  HOME = 'index',
  SEARCH = 'search',
  CATEGORY = 'category',
  PROFILE = 'profile',
}
